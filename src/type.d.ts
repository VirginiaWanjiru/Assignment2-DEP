 type NameProp={
    name:string;
   
};

type ChangeNameProps={
    name:string;
    
    
};

// Define the shape of the context data
type CounterContextType ={
    count: number;
    increment: () => void;
  };


